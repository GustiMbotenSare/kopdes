--!strict
-- src/server/DataService.luau
-- POST-SLICE (not required for the M1 vertical slice): DataStore for progress,
-- soft currency, and endings collected; OrderedDataStore for leaderboards.
-- STATUS: stub — Engineer implements after the loop is proven fun.
local DataService = {}

function DataService.init(): ()
	-- TODO(post-slice)
end

return DataService
