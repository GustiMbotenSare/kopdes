--!strict
-- src/client/CounterController.luau
-- ENG-7: counter interaction — serve / scan membership / take payment, and the
-- verb buttons that fire player intent to the server (server validates).
-- STATUS: stub — Engineer implements.
local CounterController = {}

function CounterController.init(): ()
	-- TODO(ENG-7)
end

return CounterController
